const HOME = "/";
const CONTACT = `/contact`;

const MONGODB = `/mongodb`;
const NODEJS = `/nodejs`;
const REACT = `/react`;
const JAVASCRIPT = `/javascript`;

export const routers = {
    HOME,
    CONTACT,
    MONGODB,
    NODEJS,
    REACT,
    JAVASCRIPT,
};